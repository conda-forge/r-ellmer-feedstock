{% set version = "0.4.2" %}
{% set posix = 'm2-' if win else '' %}

package:
  name: r-ellmer
  version: {{ version|replace("-", "_") }}

source:
  url:
    - {{ cran_mirror }}/src/contrib/ellmer_{{ version }}.tar.gz
    - {{ cran_mirror }}/src/contrib/Archive/ellmer/ellmer_{{ version }}.tar.gz
  sha256: 9473e96997b319a28ee9817ab387040bfe7f04ac7ddaa3d63885e055ec6f1337

build:
  number: 0
  noarch: generic
  rpaths:
    - lib/R/lib/
    - lib/
  missing_dso_whitelist:
    - '*/R.dll'        # [win]
    - '*/Rblas.dll'    # [win]
    - '*/Rlapack.dll'  # [win]

requirements:
  build:
    - {{ posix }}zip               # [win]
    - cross-r-base {{ r_base }}    # [build_platform != target_platform]
  host:
    - r-base
    - r-r6
    - r-s7 >=0.2.0
    - r-cli
    - r-coro >=1.1.0
    - r-glue
    - r-httr2 >=1.2.1
    - r-jsonlite
    - r-later >=1.4.0
    - r-lifecycle
    - r-promises >=1.5.0
    - r-rlang >=1.1.0
    - r-tibble
    - r-vctrs
  run:
    - r-base
    - r-r6
    - r-s7 >=0.2.0
    - r-cli
    - r-coro >=1.1.0
    - r-glue
    - r-httr2 >=1.2.1
    - r-jsonlite
    - r-later >=1.4.0
    - r-lifecycle
    - r-promises >=1.5.0
    - r-rlang >=1.1.0
    - r-tibble
    - r-vctrs

test:
  commands:
    - $R -e "library('ellmer')"           # [not win]
    - "\"%R%\" -e \"library('ellmer')\""  # [win]

about:
  home: https://ellmer.tidyverse.org
  dev_url: https://github.com/tidyverse/ellmer
  license: MIT
  summary: Chat with large language models from a range of providers including 'Claude' <https://claude.ai>, 'OpenAI' <https://chatgpt.com>, and more. Supports streaming, asynchronous calls, tool calling, and structured data extraction.
  license_family: MIT
  license_file:
    - '{{ environ["PREFIX"] }}/lib/R/share/licenses/MIT'
    - LICENSE

extra:
  recipe-maintainers:
    - conda-forge/r
